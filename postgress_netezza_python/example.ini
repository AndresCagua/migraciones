; Configuración para PostgreSQL
[postgresql]
; Dirección del servidor (IP o nombre de dominio)
host = localhost
; Puerto de conexión (default: 5432)
port = 5432
; Nombre de la base de datos
database = produccion_db
; Usuario con permisos
user = app_user
; Contraseña (mantener en secreto)
password = P@ssw0rdComplex123
; Esquema por defecto
default_schema = ventas

; Configuración para Netezza
[netezza]
; Servidor de Netezza
host = nz-prod-server.company.com
; Puerto (default: 5480)
port = 5480
; Nombre de la base de datos
database = DW_PROD
; Usuario
user = ETL_USER
; Contraseña
password = NZ-S3cur3Pwd!
; Nivel de seguridad (1-3)
securityLevel = 3
; Opciones SSL (prefer, require, verify-full, allow)
ssl = require
